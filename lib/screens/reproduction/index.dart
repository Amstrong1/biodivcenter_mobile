import 'package:flutter/material.dart';

class ReproductionPage extends StatelessWidget {
  const ReproductionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Reproduction Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
