import 'package:flutter/material.dart';

class SanitaryStatePage extends StatelessWidget {
  const SanitaryStatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'État Sanitaire Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
